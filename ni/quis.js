/**
 * Appends a date to the list of events.
 *
 * @param {Date} date - The date to append.
 */
function appendDate(date) {
  // Your code to append the date
  console.log(`Date appended: ${date}`);
}

// Example usage:
const today = new Date();
appendDate(today);
